# Working core (web chat + organic language learning).
# `pip install -e .` reads these from pyproject.toml; this file is kept for
# convenience / environments that expect requirements.txt.
flask>=2.3.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
python-socketio>=5.9.0
websocket-client>=1.6.0
numpy>=1.24.0

# Optional — only needed by not-yet-integrated subsystems (see SALVAGE.md):
# scikit-learn>=1.3.0
# networkx>=3.1
# faiss-cpu>=1.7.4        # vector similarity search (memory subsystem)
# psycopg2-binary>=2.9.0  # PostgreSQL (collective server)
